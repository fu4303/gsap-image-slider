{
  "private": true,
  "name": "gsap-image-slider",
  "version": "1.0.14",
  "scripts": {
    "build:studio": "npm --prefix studio run build",
    "build:web": "npm --prefix studio run build"
  },
  "devDependencies": {
    "@sanity/cli": "^1.149.13"
  }
}
